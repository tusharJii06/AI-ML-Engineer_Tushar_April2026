# Optional: OpenAI-compatible chat API (Assessment 1 & 2 agent reasoning)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# If unset, use --mock-llm for deterministic runs without network
